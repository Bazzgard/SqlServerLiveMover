<Window x:Class="SqlServerLiveMover.Gui.MessageDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="440" Width="720" MinHeight="320" MinWidth="560"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize"
        Background="{StaticResource PageBrush}" ShowInTaskbar="False">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="82" />
            <RowDefinition Height="*" />
            <RowDefinition Height="66" />
        </Grid.RowDefinitions>
        <Border Background="#102A43">
            <Grid Margin="26,0">
                <TextBlock x:Name="TitleText" Foreground="White" FontSize="22"
                           FontWeight="SemiBold" VerticalAlignment="Center" />
            </Grid>
        </Border>
        <Border Grid.Row="1" Margin="22" Background="White" CornerRadius="8" Padding="18">
            <TextBox x:Name="MessageText" IsReadOnly="True" AcceptsReturn="True"
                     TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Disabled" BorderThickness="0"
                     Background="White" FontFamily="Yu Gothic UI" FontSize="14"
                     Padding="4" />
        </Border>
        <Border Grid.Row="2" Background="White" BorderBrush="#D9E2EC" BorderThickness="0,1,0,0">
            <WrapPanel HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,22,0">
                <Button x:Name="CancelButton" Content="キャンセル" Style="{StaticResource SecondaryButton}"
                        Width="110" Click="Cancel_Click" Margin="0,0,10,0" />
                <Button x:Name="AcceptButton" Content="OK" Style="{StaticResource PrimaryButton}"
                        Width="110" Click="Accept_Click" />
            </WrapPanel>
        </Border>
    </Grid>
</Window>
